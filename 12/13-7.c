#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{
    int number,sides,dice,i;
    printf("Enter the number of sets: enter q to quit\n");
    while(scanf("%d",number))
    {
        printf("How man")
}
    return 0;
}
